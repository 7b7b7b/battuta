# Battuta 1.2.1

- macOS 与 Windows 的键盘和鼠标音效共用 16 个播放声道；快速输入时优先保留键盘声音，鼠标声音会在声道占满时自然让位。
- 静音一秒后暂停底层音频渲染，并在第一次键盘或鼠标输入时立即恢复，降低 Battuta 空闲运行时的 CPU 占用。
- Windows 便携版增强通知区域图标兼容性：启动注册会自动重试，协议协商失败不再中止启动，并可在 Explorer 重启后持续恢复图标。
- Windows 更新检查会在缓存内容不可用时主动完整刷新，避免旧 ETag 阻止获取新的 Release。
