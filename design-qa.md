# Battuta 历史页面 Design QA

## 最终结构

- 顶部左侧：过去 365 天按星期 × 小时聚合的输入节律矩阵，可切换“当前 / 差异”。
- 顶部右侧：过去 365 天与此前 365 天的应用变化表。
- 第二行：GitHub 风格的 365 日热力图，一天一个方格，按周排列并标注月份。
- 底部：年度亮点，只保留最佳一天、最长连续活跃、最常输入星期和最常输入时段。

日期选择器、7/30 天预设、月份卡片、区间概览条和活跃日期明细均已从历史页移除。

## 布局验证

- 验证窗口：1040 × 760 pt，深色外观，8 个有记录应用。
- 顶部两卡共用同一个动态高度，底边严格对齐。
- 应用标题和表头保持固定，应用行在右卡内部纵向滚动；默认高度约显示三行，不再把下方年度热力图推远。
- 应用列采用一个弹性名称列和三个有界数字列，Current / Previous / Change 均未越过右侧边界。
- 820 pt 最小窗口宽度时，顶部两卡改为纵向排列，避免压缩或横向溢出。

## 热力图几何

- 星期 × 小时矩阵与年度热力图共用 `TypingHeatmapCellMetrics`。
- 两处方格使用同一边长和同一 3 pt 间距，宽高始终相等。
- 默认宽度使用 14 pt 正方形；窗口缩小时按年度 53 周列可用宽度同步缩至最低 10 pt。
- 两张图共用 34 pt 纵轴宽度和相同的首列间距，因此周标签之后的网格起点对齐。
- 年度热力图固定显示过去 365 天，完整覆盖 53 个周列；空白日与五级 lime 强度、月份标签和少—多图例均在容器内。

## 数据与交互

- 当前区间固定为今天起向前 365 天，对比区间固定为此前 365 天。
- 节律差异按每个星期在区间内出现的次数归一化，避免 52/53 周日历组成造成虚假变化。
- 5% / 小绝对值死区映射为中性状态。
- 每个年度方格提供日期与字符数提示及辅助功能标签。
- 应用表内部滚动可访问全部应用，不再依赖拉长卡片。

## 验证结果

- Swift 6 全工程类型检查（warnings-as-errors）：通过。
- Xcode Debug 严格构建（warnings-as-errors）：通过。
- 1040 × 760 pt 真实窗口按 CGWindow ID 独立抓取验证：无裁切、无越界，两张顶卡等高。

final result: passed
